title: example
src_dir: src/en

chapters:
  - index.md

preprocessors:
    -   macros:
            macros:
                img_dir: /usr/src/app/src/img

backend_config:
  mkdocs:
    mkdocs.yml:
      theme:
        name: material
      markdown_extensions:
        - meta
